function setup() {
    let cnv = createCanvas(600, 400);
    cnv.parent("p5-canvas-parent");

    colorMode(HSB);
}
  
function draw() {
    background(30,  75, 25);
    
    noStroke();
    fill(210, 25, 75);
    rect(0, 0, width, height/1.2);

}

class Leave{
    contructor(startX, startY){

    }
}